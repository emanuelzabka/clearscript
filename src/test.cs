/*
int soma(int x, int y) does {
	print(x);
	print("\n");
	print(y);
}

main does {
	soma(10, 20);
	int contador;
	contador = 10;
	while (contador > 0) do
	{
		if (contador == 5) do {
			print("Em cinco");
		}
		print("Numero: ");
		print(contador);
		print("\n");
		contador = contador - 1;
	}
}
*/

int soma() does {
}

main does {
	soma();
}
